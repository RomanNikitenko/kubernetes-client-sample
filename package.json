{
  "name": "kubernetes-client-sample",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "compile": "tsc -b",
    "test-pod": "clear && npm run compile && node lib/pod",
    "test-custom-resource": "clear && npm run compile && node lib/custom-resource"
  },
  "author": "",
  "license": "EPL-2.0",
  "dependencies": {
    "@kubernetes/client-node": "^0.19.0"
  },
  "devDependencies": {
    "typescript": "~4.4.2"
  }
}
